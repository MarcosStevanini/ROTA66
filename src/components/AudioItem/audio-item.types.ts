export type AudioItemProps = {
  id: string;
  titulo: string;
  livro: string;
  capitulo: string;
  descricao: string;
  tema: string;
  url: string;

 }

 export type Props = {
  data:AudioItemProps
 }
